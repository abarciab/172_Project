using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TEST : MonoBehaviour 
{
    /*
    private void Update()
    {
        if (MarkerTracker.i.trackedMarkers.Contains(transform)) return;
        MarkerTracker.i.trackedMarkers.Add(transform);
    }*/
}
